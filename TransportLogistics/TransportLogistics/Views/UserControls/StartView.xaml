<UserControl x:Class="TransportLogistics.Views.UserControls.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:TransportLogistics.Infrastructure"
             xmlns:local="clr-namespace:TransportLogistics.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             DataContext="{StaticResource vmlocator}">
    

   
    <Grid Background="LightGray">
        <StackPanel Margin="70"
                    Background="AliceBlue"
                    Width="300"
                    Height="200">
            <TextBlock
                      Width="210"
                      Height="50"
                       TextAlignment="Center"
                       FontSize="30">Авторизация</TextBlock>
            <ComboBox Width="150"
                      Height="30"
                      Margin="5"
                      ></ComboBox>
            <Button Width="100"
                    Height="25"
                    Margin="25"
                    Command="{Binding AuthorizationCommand}">ok</Button>

        </StackPanel>
            
    </Grid>
</UserControl>
